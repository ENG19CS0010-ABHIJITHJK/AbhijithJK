//Write a program to add two user input numbers using one function.
#include<stdio.h>
int main()
{
  float a,b,c;
  printf("Enter number: ");
  scanf("%f", &a);
  printf("Enter number: ");
  scanf("%f", &b);
  c= a+b;
  printf(int "Sum of %f and %f is %f \n", a, b, c);
  return 0;
}
